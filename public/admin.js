// The instructor console is built in a later milestone. This placeholder keeps
// the script present so the page loads; app.js falls back to it for non-student
// routes.
(function () {
  "use strict";
  // SG.renderAdmin is defined in a later milestone.
})();
